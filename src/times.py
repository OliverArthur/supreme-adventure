def convert_to_minutes(hours):
    """(int) -> int
    Conver the hours to minutes with the givens hours
    >>> convert_to_minutes(2)
    120
    >>> convert_to_minutes(3)
    180
    """
    result = hours * 60
    return result


    def get_capital(capital):
        """(str) -> str
        >>> get_capital(capital)
        Amnsterdam
        >>> get_capital(capital)
        Roma
        """
        return capital

