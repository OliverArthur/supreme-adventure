def area(base, height):
    """(number, number) -> number
    Return the area of triangle with given the base and heigth
    >>> area(10, 40)
    200
    >>> area(30, 60)
    900
    """
    return base * height / 2
